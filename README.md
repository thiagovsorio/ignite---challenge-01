# To-Do backend
A To-Do backend. Handles the Creation, Read, Update and Delete of the "todos", which are arrays in the user objects
Handles the Creation and Read of the user.

It was a simple "challenge" from a course, my job was to simply implement the methods above, which were empty.
